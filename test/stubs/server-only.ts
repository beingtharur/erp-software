// Stub for the "server-only" package under Vitest.
// Next.js aliases this to a throwing module only inside client bundles;
// in a plain Node test run there's no bundler doing that, so we no-op it.
export {};
